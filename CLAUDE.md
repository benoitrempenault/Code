# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository is currently empty — no source files, no commits, no build tooling, no README. There is no architecture, no test suite, and no command set to document yet.

When real code lands, update this file with:
- Build / lint / test commands (including how to run a single test).
- The high-level architecture that spans multiple files and isn't obvious from directory browsing.
- Anything project-specific a new contributor couldn't infer from reading one file.
