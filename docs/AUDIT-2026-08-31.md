# Audit Studio Kadima — 31 août 2026

Rapport complet (page partageable) : https://claude.ai/code/artifact/a67d20f1-336d-4a24-b18e-2fcd073dd640
Décision de Benoît : on reprend le 18 septembre 2026 (retour de congé). Relance planifiée.

## État au 31 août
- 630 tests serveur verts à chaque publication ; 132 routes (40 admin, 27 membre, 45 session, 20 publiques/clé).
- Livré en prod : fil de suivi + rappels, fiche adresse cliquable (maisons vertes, habitants, estimés/mandats/ventes,
  qualifier A/B/C), 141 îlots CenturyNet (fichier ilots-kadima.json à importer), estimation enrichie + brochure,
  fiche prestations → contact vendeur, acheteurs (chambres/séjour, plu/pas plu, relance directe stock),
  envois individuels mail/SMS, bandeau de compte partout.

## Sécurité — constats
1. AUJOURD'HUI : le mot de passe AMEPI (Amanda) a transité en clair dans la conversation → à changer.
   (fichiers de travail détruits, rien dans le dépôt — vérifié). Jeton CenturyNet expiré de lui-même.
2. Moyen : pas de plafond quotidien sur les envois individuels / création de prospects par membre.
3. Moyen : tout membre peut supprimer n'importe quel suivi/visite, sans corbeille ni contrôle d'auteur.
4. Moyen : sessions 30 jours (localStorage) — trop long pour tablettes partagées ; pas de « déconnecter partout ».
5. Moyen : RGPD — pas d'export/effacement d'une personne, pas de règle de conservation.
6. Faible : photo brochure insérée sans escH côté client (serveur valide le préfixe data:image).
7. Faible : pas d'en-têtes HSTS / X-Content-Type-Options sur l'API.
8. Faible/choix : chaque conseiller voit toute la base (carte, listes).

## Code — dette
- app.js 3 291 l., crm.js 2 127 l., test.mjs 2 784 l. → découper par domaine (sans changer le comportement).
- Utilitaires front dupliqués (api ×6, toast ×5, escH ×3, libellés suivis ×3) → module commun racine
  (le bandeau assets/js/compte-bandeau.js prouve que ça marche).
- Schéma append-only → tables satellites JSON ; mettre en place les migrations D1 versionnées.
- Tests navigateur (Playwright) hors dépôt → les verser sous server/smoke/ + CI.
- Chargements complets (carte 38k points, contacts) : OK aujourd'hui, à surveiller vers 60k.

## Écarts cahier des charges
- « Mes rappels » par conseiller + mail du matin ; rattacher un contact existant à une adresse ;
  chambres/séjour non exploités par le rapprochement (annonces sans ces données) ;
  AMEPI/Yanport en attente ; SMS en attente clé Brevo ; Administration peu adaptée au téléphone ;
  pas de journal des modifications.

## Feuille de route (dans l'ordre)
1. Durcir (1–2 j) : plafond envois/jour, suppression auteur/admin + corbeille, sessions conseiller 7 j +
   « déconnecter partout », export/effacement RGPD, escH photo, en-têtes HTTP, smokes dans le dépôt.
2. Élargir : AMEPI dans le stock (réglage relance/signalement), Yanport → écran « Opportunités de délégation »
   (jamais de mail client sans mandat), mes rappels + mail du matin, rattacher habitant existant, admin mobile.
3. Consolider : découpage serveur, module commun, migrations D1, journal des modifications,
   visibilité par conseiller, mode hors-ligne terrain.

## Actions Benoît (6)
1. Changer le mot de passe Amanda (AMEPI).
2. Poser BREVO_API_KEY sur Cloudflare (studio-brochure-api → Variables et secrets).
3. Activer le parcours estimation (Administration → Estimations).
4. Importer ilots-kadima.json (Studio Prospection → Importer des îlots).
5. Demander à Yanport : API ou export des recherches inclus ?
6. Export d'une recherche AMEPI (ou requête /search capturée) pour brancher le fichier des mandats.

## Notes techniques AMEPI (Amanda)
- ASP.NET Core, login formulaire (Email/Password/SelectedAgency/__RequestVerificationToken), session cookie.
- Recherche : POST /search (JSON {value:[…], total}), détail /mandate/details/{id},
  /api/cities-mandate-search, /agency/mandates. Le corps exact de /search reste à capturer depuis un vrai
  navigateur (le proxy du bac à sable refuse Playwright vers amepi.info).
